{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tianyihuang.com/data/site-content.schema.json",
  "title": "Personal site content",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "maintenance",
    "site",
    "profile",
    "about",
    "publications",
    "projects",
    "misc",
    "blog",
    "cv"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "maintenance": {
      "type": "boolean"
    },
    "site": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "subtitle"],
      "properties": {
        "name": { "type": "string" },
        "subtitle": { "type": "string" }
      }
    },
    "profile": {
      "type": "object",
      "additionalProperties": false,
      "required": ["photo", "wasted_photo", "hp_max", "hp_damage", "name", "role", "school", "links"],
      "properties": {
        "photo": { "type": "string", "description": "Site-relative path to the profile photo (e.g. data/images/profile/me.jpg)." },
        "wasted_photo": { "type": "string", "description": "Site-relative path to the 'wasted' game-over photo." },
        "hp_max": { "type": "number", "exclusiveMinimum": 0 },
        "hp_damage": { "type": "number", "exclusiveMinimum": 0 },
        "name": { "type": "string" },
        "role": { "type": "string" },
        "school": { "type": "string" },
        "links": {
          "type": "array",
          "items": { "$ref": "#/$defs/link" }
        }
      }
    },
    "about": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "object",
          "additionalProperties": false,
          "required": ["experience", "education"],
          "properties": {
            "experience": { "$ref": "#/$defs/experienceSection" },
            "education": { "$ref": "#/$defs/educationSection" }
          }
        }
      }
    },
    "publications": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "array",
          "items": { "$ref": "#/$defs/publication" }
        }
      }
    },
    "projects": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "array",
          "items": { "$ref": "#/$defs/project" }
        }
      }
    },
    "misc": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "array",
          "items": { "$ref": "#/$defs/miscItem" }
        }
      }
    },
    "blog": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "array",
          "items": { "$ref": "#/$defs/blogPost" }
        }
      }
    },
    "cv": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "content"],
      "properties": {
        "title": { "type": "string" },
        "description": { "$ref": "#/$defs/sectionDescription" },
        "content": {
          "type": "object",
          "additionalProperties": false,
          "required": ["url"],
          "properties": {
            "url": { "type": "string", "description": "Site-relative path (or full URL) to the CV PDF (e.g. data/resume/Resume_V4.pdf)." }
          }
        }
      }
    }
  },
  "$defs": {
    "sectionDescription": {
      "oneOf": [
        { "type": "string" },
        {
          "type": "array",
          "items": { "type": "string" }
        }
      ]
    },
    "experienceSection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "items"],
      "properties": {
        "title": { "type": "string" },
        "items": {
          "type": "array",
          "items": { "$ref": "#/$defs/experience" }
        }
      }
    },
    "educationSection": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "items"],
      "properties": {
        "title": { "type": "string" },
        "items": {
          "type": "array",
          "items": { "$ref": "#/$defs/education" }
        }
      }
    },
    "link": {
      "type": "object",
      "additionalProperties": false,
      "required": ["label", "url"],
      "properties": {
        "label": { "type": "string" },
        "url": { "type": "string" }
      }
    },
    "education": {
      "type": "object",
      "additionalProperties": false,
      "required": ["degree", "school", "date", "details", "logo"],
      "properties": {
        "degree": { "type": "string" },
        "school": { "type": "string" },
        "date": { "type": "string" },
        "details": { "type": "string" },
        "logo": { "type": "string", "description": "Site-relative path to the school logo image (e.g. data/images/logos/nus_logo.png)." }
      }
    },
    "experience": {
      "type": "object",
      "additionalProperties": false,
      "required": ["role", "org", "date", "meta", "description"],
      "properties": {
        "role": { "type": "string" },
        "org": { "type": "string" },
        "date": { "type": "string" },
        "meta": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "publication": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "authors", "conference", "teaser", "abstract", "links"],
      "properties": {
        "title": { "type": "string" },
        "authors": { "type": "string" },
        "conference": { "type": "string" },
        "teaser": { "type": "string", "description": "Site-relative path to the publication teaser image." },
        "poster": { "type": "string", "description": "Site-relative path to the publication poster image (optional)." },
        "abstract": { "type": "string" },
        "links": {
          "type": "object",
          "additionalProperties": { "type": "string" }
        }
      }
    },
    "project": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "date", "description", "tags", "run_url"],
      "properties": {
        "title": { "type": "string" },
        "date": { "type": "string" },
        "description": { "type": "string" },
        "tags": {
          "type": "array",
          "items": { "type": "string" }
        },
        "run_url": { "type": "string", "description": "Site-relative path (or full URL) to a runnable demo page (e.g. projects/qsynth/index.html)." }
      }
    },
    "miscItem": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "description", "module"],
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "module": {
          "oneOf": [
            { "$ref": "#/$defs/galleryModule" },
            { "$ref": "#/$defs/youtubeModule" },
            { "$ref": "#/$defs/mapModule" }
          ]
        }
      }
    },
    "galleryModule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "items"],
      "properties": {
        "type": { "const": "gallery" },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["src", "alt"],
            "properties": {
              "src": { "type": "string", "description": "Site-relative path to the gallery image." },
              "alt": { "type": "string" }
            }
          }
        }
      }
    },
    "youtubeModule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "url"],
      "properties": {
        "type": { "const": "youtube" },
        "url": { "type": "string" }
      }
    },
    "mapModule": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "places"],
      "properties": {
        "type": { "const": "map" },
        "places": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["name", "lat", "lon"],
            "properties": {
              "name": { "type": "string" },
              "lat": { "type": "number", "minimum": -90, "maximum": 90 },
              "lon": { "type": "number", "minimum": -180, "maximum": 180 }
            }
          }
        }
      }
    },
    "blogPost": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "cover", "intro", "markdown"],
      "properties": {
        "title": { "type": "string" },
        "cover": { "type": "string", "description": "Site-relative path to the post cover image (e.g. posts/my-post/cover.jpg)." },
        "intro": { "type": "string" },
        "markdown": { "type": "string", "description": "Site-relative path to the post markdown file." }
      }
    }
  }
}
